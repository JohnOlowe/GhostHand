<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!--
      Framework-only theming.

      The parent is android:Theme.Material, which ships in the OS: no AppCompat and
      no com.google.android.material dependency. That is a deliberate trade - it is
      what lets this project be built by toolchain/build.sh (aapt2 + ECJ + D8) with
      nothing but the platform android.jar, since AndroidX artifacts cannot be
      fetched in a locked-down environment.

      Every override below is an android:* framework attribute, so the OS widgets
      (Button, EditText, Switch, Spinner, ProgressBar) pick up the palette for free.
    -->
    <style name="AppTheme" parent="@android:style/Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/bg</item>
        <item name="android:colorBackground">@color/bg</item>
        <item name="android:colorPrimary">@color/surface_alt</item>
        <item name="android:colorPrimaryDark">@color/bg</item>
        <item name="android:colorAccent">@color/accent</item>
        <item name="android:statusBarColor">@color/bg</item>
        <item name="android:navigationBarColor">@color/bg</item>
        <item name="android:textColorPrimary">@color/text_primary</item>
        <item name="android:textColorSecondary">@color/text_secondary</item>
        <item name="android:textColorHint">@color/text_secondary</item>
        <item name="android:colorControlNormal">@color/text_secondary</item>
        <item name="android:colorControlActivated">@color/accent</item>
    </style>

    <!-- ============================== text =================================
         Dot-separated style names inherit implicitly from the part before the dot,
         and aapt2 REQUIRES that base style to exist. So Text and Stat are declared
         first; Text.Title then picks up their common attributes for free.
    -->

    <style name="Text">
        <item name="android:textColor">@color/text_primary</item>
        <item name="android:textSize">14sp</item>
    </style>

    <style name="Text.Title">
        <item name="android:textColor">@color/text_primary</item>
        <item name="android:textSize">20sp</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="Text.Subtitle">
        <item name="android:textColor">@color/text_secondary</item>
        <item name="android:textSize">14sp</item>
    </style>

    <style name="Text.Label">
        <item name="android:textColor">@color/text_secondary</item>
        <item name="android:textSize">11sp</item>
        <item name="android:textAllCaps">true</item>
        <item name="android:letterSpacing">0.08</item>
    </style>

    <style name="Text.Mono">
        <item name="android:fontFamily">monospace</item>
        <item name="android:textColor">@color/text_primary</item>
        <item name="android:textSize">13sp</item>
    </style>

    <style name="Stat">
        <item name="android:textColor">@color/accent</item>
        <item name="android:fontFamily">monospace</item>
    </style>

    <style name="Stat.Value">
        <item name="android:textColor">@color/accent</item>
        <item name="android:fontFamily">monospace</item>
        <item name="android:textSize">20sp</item>
        <item name="android:textStyle">bold</item>
    </style>

    <!-- ============================ containers ============================= -->

    <style name="Card">
        <item name="android:background">@drawable/bg_card</item>
        <item name="android:padding">16dp</item>
    </style>

    <!-- Explicit parent: "Card.Clickable" would otherwise need a style named Card,
         which does exist above, but being explicit keeps the intent obvious. -->
    <style name="Card.Clickable" parent="Card">
        <item name="android:background">@drawable/bg_card_clickable</item>
        <item name="android:clickable">true</item>
        <item name="android:focusable">true</item>
    </style>

    <!-- ============================== widgets ============================== -->

    <!--
      Framework Buttons tint their background with colorAccent, which is a bright
      green here, so the label has to be forced dark to stay readable.
    -->
    <style name="Button.Primary" parent="@android:style/Widget.Material.Button">
        <item name="android:textColor">#04140D</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:textSize">15sp</item>
    </style>

    <style name="Button.Quiet" parent="@android:style/Widget.Material.Button.Borderless">
        <item name="android:textColor">@color/accent</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:textSize">14sp</item>
    </style>
</resources>
